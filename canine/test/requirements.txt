# Test dependencies for canine on Python 3.14 (WolF 2.0).
#
# Mirrors [project].dependencies in pyproject.toml, except that dalmatian and
# slurm_gcp_docker are left out: they cannot be installed locally, and
# conftest.py (repo root) stubs them. Keep the two in step when a pin changes.
#
# Setup, from the canine/ repo root:
#   uv venv --python 3.14 .venv-test
#   uv pip install --python .venv-test/bin/python -r canine/test/requirements.txt
#
# Running: see canine/test/README.md.

# pyproject.toml
paramiko>=2.5.0
pandas>=2.2, <4.0
numpy>=2.0
google-auth>=1.6.3
PyYAML>=5.1
agutil>=4.1.0
hound @ git+https://github.com/getzlab/hound.git@wolf-2.0-update
google-api-python-client>=1.7.11
docker>=4.1.0
psutil>=5.6.7
port-for>=0.4
tables>=3.6.1
google-crc32c>=1.5.0
google-cloud-compute>=1.6.1
google-cloud-storage>=2.0.0
requests>=2.30.0, <3.0.0
urllib3>=1.26, <3
crayons>=0.2.0

# test runners
pytest
timeout-decorator
